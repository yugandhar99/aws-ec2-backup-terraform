# Screenshots to Add Later

Add these screenshots after deploying in a sandbox AWS account:

1. AWS Backup plan showing daily schedule
2. Backup vault with KMS encryption
3. Backup selection using the `BackupPlan=daily-backup` tag
4. Successful backup job
5. Recovery points in backup vault
6. SNS notification email or topic
7. GitHub Actions Terraform quality check run
8. Drift detection workflow run
9. Optional generated backup summary report

Store images under:

```text
static/ss/
```

Then reference them in the main README.
